#include <stdio.h>

int main() {
    
    char ch;
    scanf("%c",&n);
    if (ch>=65 && ch<=90)
    printf("Uppercase");
    else if(ch>=97 && ch<=122)
    printf("Lowercase");
    return 0;
}